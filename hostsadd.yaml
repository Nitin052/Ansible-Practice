---
  - name: "NETWORK SETUP"
    hosts: localhost
    gather_facts: no
    tasks:
    - name: "add hosts" 
      tower_host:
          name: "192.2.3.4"
          inventory: "Test_inventory"
          tower_host: ""
          tower_password:
          tower_username: 
          variables: 
             ansible_become: true
             ansible_become_method: enable
        

