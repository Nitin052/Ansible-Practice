---
  - name: "NETWORK SETUP"
    hosts: localhost
    gather_facts: no
    tasks:
    - name: Login to a form based webpage.
      uri:
        url: http://10.136.59.117/api/v2/inventories/23/hosts/
        method: POST
        body_format: form-urlencoded
        body:
          name: 192.3.45.5
          enabled: true
        status_code: 301
      register: login
